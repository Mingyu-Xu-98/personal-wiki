# System Meta Skill

System skills are durable operating procedures for agents and maintainers.

They should be:

- close to the agent that uses them
- written in markdown
- versioned with code
- covered by evals when they affect behavior

In the eve-style structure, broad project rules live in docs, while agent-specific procedures live under `agents/*/skills/`.
